#include <stdio.h>

int main(void)
{
    // int i = 2,j;
    // // j=i++ + i++ + ++i + i++ + i++;
    // for(j=1;j<=5;j++){
    //     i=i++;
    // }
    // printf("%d%d", i,j);

    // int a=0,b=1,c=4,k;
    // k=a++ || ++b && ++c;
    // printf("%d%d%d%d",a,b,c,k);

    // int i=2,j=3;
    // i=i++ + j++;
    // printf("%d %d", i, j);

    // int i=2;
    // printf("%d %d %d",i++,++i,i++);

    int a=10,b=20,c=0;
    if(a,b,c)
        printf("Welcome");
    else
        printf("Invalid");
    return 0;
}